type Mutation {
  sendInvite(input: SendInviteInput!): ResponsePayload! @auth
  cancelInvite(input: SendInviteInput!): ResponsePayload! @auth
}

input SendInviteInput {
  device: ID!
  email: String!
}

