enum SWITCH {
  ON
  OFF
}

input SwitchInput {
  turn: SWITCH!
  device: ID!
}

type Mutation {
  switch(input: SwitchInput!): Boolean @auth
}

type SwitchedPayload {
  turned: SWITCH
  device: Device
}

type Subscription {
  switched: SwitchedPayload
}