enum SWITCH {
  ON
  OFF
}

input SwitchInput {
  turn: SWITCH!
  device: ID!
}

type Mutation {
  switch(input: SwitchInput!): Boolean @auth
}

type SwitchedPayload {
  turned: SWITCH!
  relay: Int!
}

type Subscription {
  switched: SwitchedPayload!
}